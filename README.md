# 黑箱入侵：404層

一隻可直接用瀏覽器玩的 3D 風格黑客解謎驚嚇小遊戲。

## 玩法

- 用 iPhone 橫向開啟最佳。
- 觸控按鈕或鍵盤控制移動。
- 破解三個黑箱節點：A、B、C。
- 三個節點完成後，搵到 `EXIT` 離開 404 層。

## 控制

- iPhone：用畫面底部按鈕轉向、前進、後退、破解、開關聲音、全屏。
- 電腦：`WASD` / 方向鍵移動，`E` / `Enter` / 空白鍵互動，`M` 聲音，`F` 全屏。

## 部署

GitHub Pages 可以直接部署 repo root。無需 build step。
